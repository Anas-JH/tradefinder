# Set to "false" to place real calls via CALL-E. Defaults to dry-run (fake, staggered results).
DRY_RUN=true

# Required when DRY_RUN=false. Get this from the CALL-E dashboard.
CALLE_API_KEY=

# Optional override for the CALL-E API base URL.
CALLE_BASE_URL=https://api.heycall-e.com
